# Canvas 오류 분석 및 수정 보고서

분석 대상은 GitHub Pages에서 제공되던 `index.html`, 과거 사본 `Canvas.html`, PWA 설정, Supabase 접근 방식입니다. 단순 화면 오류뿐 아니라 권한 우회, 데이터 노출, 실시간 동기화 실패 가능성까지 함께 점검했습니다.

## 주요 오류와 원인

| 등급 | 발견한 문제 | 실제 영향 | 적용한 수정 |
|---|---|---|---|
| 매우 높음 | 비공개방 비밀번호를 평문으로 저장하고 방 목록에서 읽음 | 로그인 사용자에게 암호가 노출되고 브라우저에서 검사를 우회할 수 있음 | `workspace_rooms.password_hash`에 bcrypt 해시만 저장하고 서버 RPC에서 비교 |
| 매우 높음 | 방장·관리자를 닉네임이나 하드코딩 이메일로 판단 | 닉네임 중복·변경·조작으로 강퇴, 타이머, 관리자 기능의 권한이 흔들림 | Auth UUID와 DB의 `role`을 서버가 확인하도록 변경 |
| 매우 높음 | 공개 Realtime 채널 사용 | 프로젝트 키를 아는 외부인이 방/시스템 이벤트를 위조할 가능성 | 모든 채널을 private로 전환하고 `realtime.messages` RLS 정책 추가 |
| 높음 | 채팅과 강퇴/타이머가 같은 클라이언트 송신 채널에 존재 | 일반 방 구성원이 관리자 이벤트나 다른 사람 채팅을 가장할 수 있음 | 채팅·타이머·강퇴를 `room-server:*` 읽기 전용 채널과 검증 RPC로 분리 |
| 높음 | 게시글·댓글·채팅·사용자·작품 정보를 `innerHTML`에 삽입 | 저장형/실시간 XSS 가능 | 동적 콘텐츠를 `textContent`와 DOM 노드로만 렌더링 |
| 높음 | 새로고침 시 세션을 복구하지 않고 로그아웃은 페이지만 새로고침 | 로그인 화면이 잘못 나타나거나 세션이 남음 | `getSession()` 복구와 `auth.signOut()` 구현 |
| 높음 | 게스트도 온라인 방·채널 기능과 섞임 | 인증 없는 사용자 상태가 협업 데이터에 들어갈 수 있음 | 익명 Auth UUID와 회원 전용 DB 규칙을 적용해 공개방 협업 허용, 혼자 그리기는 별도 제공 |
| 높음 | 캔버스 전체 Data URL을 Broadcast 한 메시지로 전송 | 그림이 커지면 메시지 제한·타임아웃으로 동기화가 끊김 | WebP 상태를 32KB 단위, 최대 6MB/192조각으로 분할하고 순서·크기·대상 검증 |
| 높음 | CSS로 줄어든 캔버스에서 마우스/터치 좌표를 그대로 사용 | 모바일·작은 화면에서 실제 선이 포인터와 어긋남 | Pointer Events와 내부 1100×700 좌표 변환 적용 |
| 중간 | 참여자 목록을 클라이언트의 join/present 메시지로 구성 | 유령 참가자, 중복 닉네임, 참여자 위조 | Presence 상태와 서버 회원 목록을 교차 확인한 UUID→닉네임 Map 사용 |
| 중간 | 작품 소유권을 닉네임으로 검색·저장 | 닉네임 변경/중복 시 다른 사람 작품이 섞임 | `owner_id`, `room_id`, UUID별 Storage 경로와 RLS 적용 |
| 중간 | 이미지·텍스트·브러시 실시간 입력을 검증하지 않음 | 비정상 크기·좌표·색상·문자열로 렌더링 오류나 자원 낭비 | 좌표/굵기/색상/브러시/문자열/조각 수를 수신 측에서도 제한 |
| 중간 | 회원가입 뒤 프로필을 브라우저가 직접 삽입 | 이메일 확인 설정에 따라 프로필 누락 또는 중복 충돌 | `auth.users` 트리거가 프로필을 생성하고 기존 계정도 이관 |
| 중간 | 닉네임 변경 시 서버 형식 제한이 없음 | 비어 있거나 지나치게 긴 닉네임이 방·게시글 화면을 깨뜨릴 수 있음 | DB 정책에서도 한글·영문·숫자와 `_.-` 2~20자만 허용 |
| 중간 | 긴 보안 마이그레이션이 한 작업으로 묶이지 않음 | 중간 구문 실패 때 일부 정책만 적용될 수 있음 | 전체 마이그레이션을 트랜잭션으로 묶어 실패 시 모두 되돌림 |
| 중간 | 기존 운영 데이터와 평문 비밀번호를 고려하지 않은 일괄 구조 변경 | 자료 유실 또는 이전 뒤에도 평문 비밀번호가 남을 수 있음 | 비밀번호는 bcrypt 해시로만 백업·이전하고 원문 삭제, 소유자 불일치 시 전체 작업 자동 취소 |
| 중간 | 보안 정의자 뷰로 닉네임 검색 | 뷰가 기본적으로 RLS를 우회해 프로필 범위가 넓어질 수 있음 | 로그인·활성 상태와 정확한 닉네임 일치를 검사하는 최소 반환 RPC로 교체 |
| 낮음 | 새 외래 키에 보조 인덱스가 없음 | 작품·댓글·신고가 늘면 소유자/방 기준 조회와 삭제가 느려질 수 있음 | 배포 후 성능 감사에서 확인된 외래 키 7개에 인덱스 추가 |
| 중간 | 관리자 목록에서 사용자 이메일을 읽음 | 필요 없는 개인정보가 브라우저로 전달됨 | 관리자 UI도 UUID·닉네임·역할·차단 상태만 조회 |
| 중간 | PWA manifest가 `/`를 시작 주소로 사용하고 서비스 워커를 등록하지 않음 | 설치 앱이 Canvas가 아닌 루트로 열리고 오프라인 셸이 작동하지 않음 | `./` 범위, 아이콘 링크, 버전 캐시, 서비스 워커 등록 추가 |
| 중간 | 과거의 취약한 `Canvas.html`이 공개 경로에 남음 | 사용자가 오래된 안전하지 않은 화면에 직접 접근 가능 | 최신 앱으로 즉시 이동하는 호환 리디렉터로 교체 |
| 낮음 | 모바일 모달·툴바·캔버스가 고정 폭이고 확대가 제한됨 | 일부 기기에서 도구가 잘리거나 접근성이 나쁨 | 확대 허용, 반응형 그리드/툴바/모달/캔버스 적용 |
| 낮음 | 저장 실패 시 업로드 파일만 남을 수 있음 | Storage 고아 파일 누적 | DB 저장 실패 시 방금 업로드한 객체를 즉시 정리 |
| 중간 | 로그인·가입에서 비밀번호 공백을 제거하지만 재설정에서는 보존 | 앞뒤 공백이 있는 비밀번호를 재설정한 뒤 로그인 불가 | 비밀번호는 입력값 그대로 전달하고 이메일·닉네임만 정리 |
| 중간 | Auth SDK가 재설정 URL을 먼저 처리하면 복구 화면을 놓침 | 메일 링크에서 비밀번호 변경 대신 로비로 이동할 수 있음 | SDK 초기화 전 복구 상태 보존과 PASSWORD_RECOVERY 이벤트 처리 |
| 높음 | 대기 중인 이미지 동기화가 현재 방 채널을 매번 다시 참조 | 방 이동 중 이전 그림이 새 방으로 전송되거나 덮어써질 수 있음 | 전송 시점의 방·채널·사용자를 고정하고 이미지 디코딩 완료 시에도 재검사 |
| 중간 | 조각 묶음에 보낸 사람 검사가 없음 | 서로 다른 사용자의 조각이 같은 전송에 섞일 수 있음 | 전송 ID와 함께 발신자·대상·방을 대조 |
| 중간 | 서비스 워커가 같은 도메인의 캐시를 모두 삭제 | 다른 GitHub Pages 프로젝트의 오프라인 데이터에 영향 | Canvas 접두사 캐시만 갱신하고 /Canvas/ 경로에만 적용 |

## 권한 경계

- 브라우저는 공개용 publishable key만 사용합니다.
- 방 비밀번호 원문과 해시는 RPC 반환값에 포함되지 않습니다.
- 기존 평문 방 비밀번호는 해시 이전 성공 뒤 원본 열에서도 삭제됩니다.
- `workspace_rooms`와 `workspace_room_members`는 브라우저가 직접 읽거나 쓰지 못하며 보안 함수만 접근합니다.
- 채팅, 강퇴, 타이머는 서버가 보낸 전용 채널 이벤트만 UI가 처리합니다.
- 작품/게시글/댓글/신고는 `auth.uid()`와 행 소유자를 대조하는 RLS가 적용됩니다.
- Realtime 정책은 방 구성원 여부를 확인합니다. 실제 적용을 위해 Dashboard의 **Allow public access**도 반드시 꺼야 합니다.

## 검증 결과

- Node 구문 검사: 통과
- 자동 회귀 테스트: 20개 (문법·보안 검사와 실행 회귀 테스트 포함)
- PostgreSQL 호환 문법 파서로 마이그레이션 본문을 검사하고, 트랜잭션 경계와 추가 정책은 자동 회귀 검사에 포함
- PWA manifest JSON 파싱: 통과
- 과거 평문 방 테이블 접근, 전체 행 `select('*')`, 공개 방 채널, 단일 대용량 동기화 이벤트가 제거되었음을 정적 검사
- 동기화 조각이 모두 모이기 전에는 렌더링하지 않는 동작을 실행 테스트
- Git diff 공백/충돌 검사 포함

운영 DB의 사용자 3명, 방 2개, 작품 2개, 게시글 1개, 댓글 1개가 보존되고 관리자 1명과 비공개 백업이 있으며 평문 방 비밀번호가 0개임을 확인했습니다.

2026-09-08: 기존 보안 수정본을 GitHub main에 병합하고 공개 사이트에서 그리기·되돌리기·다시 실행을 확인했습니다. Auth 주소 설정을 확인했고 Realtime 공개 접근을 비활성화했습니다. 날짜 파티션을 복구한 후 DB 진단 메시지를 브라우저에서 실제 수신했습니다.

2026-09-09: 요청에 따라 온라인 게스트 협업을 추가했습니다. 서버 마이그레이션 적용과 SQL 통합 검사가 통과했습니다. 공개방 목록·입장·참여자·채팅·신고·퇴장은 허용되며, 방 생성·비공개방·게시글 저장·이미지 업로드·프로필 변경은 차단됩니다. 조작된 게스트 메타데이터와 클레임으로 회원 권한을 얻지 못하고, 방 비공개 전환 시 게스트를 제거하는 것도 확인했습니다. 테스트 자료는 모두 롤백되어 운영 행 수가 유지됐습니다.

익명 로그인 활성화와 신규 게스트 화면의 실제 동시 접속 검증은 아직 남았습니다. SQL 통합 검사가 사용자 브라우저의 전체 흐름 검증을 대신하지 않습니다. `DEPLOYMENT_CHECKLIST.md`를 따릅니다.
